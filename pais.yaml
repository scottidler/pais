# PAIS Configuration
# Personal AI System

# Log level: trace, debug, info, warn, error, off
# Can be overridden by RUST_LOG env var
log-level: debug

paths:
  plugins: ~/.config/pais/plugins
  skills: ~/.config/pais/skills
  history: ~/.config/pais/history

hooks:
  security-enabled: true
  history-enabled: true
  ui-enabled: true

observability:
  enabled: true
  sinks:
    - file

environment:
  repos-dir: ~/repos/

  tool-preferences:
    ls: eza
    tree: eza --tree
    grep: rg
    find: fd
    cat: bat

  tools:
    otto:
      github: otto-rs/otto
      description: CI runner, task automation
    clone:
      github: scottidler/clone
      description: Git clone to repos-dir/{org}/{repo}

# MCP (Model Context Protocol) server configuration
mcp:
  # Source files for MCP server definitions (first one with a server wins)
  sources:
    - ~/.mcp.json

  # Default profile when none specified (empty = no MCPs for minimal sessions)
  default-profile: minimal

  # Named profiles
  profiles:
    minimal: []
    github:
      - multi-account-github
    slack:
      - slack
    work:
      - multi-account-github
      - slack
      - atlassian
    all:
      - multi-account-github
      - slack
      - atlassian

  # Additional server definitions (supplements sources)
  servers:
    multi-account-github:
      command: multi-account-github-mcp
      args:
        - serve
    atlassian:
      command: npx
      args:
        - -y
        - "@anthropic/mcp-server-atlassian"
